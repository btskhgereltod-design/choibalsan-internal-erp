# OVERVA Work Log — 2026-08-28

## Public V25 confirmed requirement artifact

- Added an explicit human confirmation action to the browser-local request
  detail surface. Confirmation creates a new immutable-by-revision draft state
  with `requirement-confirmed`, artifact version, and confirmation time.
- Added deterministic UTF-8 text export containing request identity, revision,
  confirmation time, unpublished/project-not-created boundaries, and the full
  structured request package.
- Kept the boundary explicit: confirmation does not publish a request, send it
  to a provider, create a project, or write to the server.
- Preserved existing request registries and drafts without migration or data
  deletion. Existing drafts remain drafts until their user confirms them.
- Cache-busted the public assets to V25 and the request registry contract to V3.
- Verified `site.js` and `request-draft-registry.js` syntax, the focused 12-test
  public flow, `git diff --check`, the complete 192-test API/repository suite,
  and a production-equivalent `overva-public-site:v25-local` Docker image build.
- Verified pre-deployment backup `overva-20260828T092432Z` passed database,
  uploads, metadata checksums, PostgreSQL archive listing, and uploads archive
  inspection.
- Rebuilt and recreated only production `public-site` with the Cloudflare Compose
  overlay. Caddy, API, tenant web, database, backup scheduler, and monitor were
  not recreated; all seven services remained healthy.
- External Home, V25 JavaScript/CSS, request registry V3, status health, and
  Caddy loopback returned HTTP 200. Live HTML exposed the V25 asset and
  confirmation/download controls; the registry exposed the confirmation and
  artifact contracts.
- Real-user requirement-accuracy measurement remains pending.

## D-023 Platform, Market, and vendor separation

- Accepted that `app.overva.com` Platform/App Factory and the multi-supplier
  OVERVA Market are separate businesses, data boundaries, and administrative
  contexts.
- Defined `OVERVA Apps` as a normal Market supplier that may sell
  Platform-produced apps and bid on freelance orders without ranking, fee,
  review, enforcement, policy, or data-access privilege.
- Replaced D-020's mandatory official-control-record interpretation. Market
  governance now means integrity, confidentiality, moderation, verified
  evidence, and policy enforcement—not controlling customer choice or forcing
  Platform adoption.
- Added `MARKET_PLATFORM_SEPARATION_CONTRACT_V1.md` and aligned architecture,
  vision, market-entry strategy, marketplace operating model, public portfolio
  contract, demo audit, current state, and repository agent memory.
- Kept implementation truth explicit: no server-backed multi-supplier Market,
  product commerce, freelance proposal system, forum, payments, or neutral
  ranking engine is currently claimed.
- Added a repository contract test for role, data, equal-participation, and
  implementation-truth boundaries; the complete 193-test suite passes.

## Public Home V26 three-area market shell

- Reframed the first public screen around three plain-Mongolian primary areas:
  `Маркет`, `Форум`, and `Захиалгат ажил`; Market is the default arrival area.
- Added a responsive sample product grid with category and text filtering.
  OVERVA Apps products and independent-supplier products use the same card
  structure, with OVERVA Apps identified only as a supplier.
- Moved the existing customer/provider request board behind the custom-work
  area so its role switch, request actions, and lifecycle do not blur product
  commerce or community discussion.
- Added a separate forum preview with topic search. Product cards and forum
  topics are visibly labelled as samples; live pricing, purchase, posting,
  accounts, and supplier onboarding remain disabled and are not claimed.
- Added area-aware search, direct `?area=` selection, responsive navigation,
  V26 asset cache keys, and regression assertions for the three areas and truth
  boundary.
- Verified JavaScript syntax, the focused 12-test public flow, all 193 repository
  tests, and the production-equivalent `overva-public-site:v26-local` image.
- Created and verified pre-deployment backup `overva-20260828T101326Z`:
  database dump, uploads archive, metadata checksums, PostgreSQL archive listing,
  and uploads archive inspection all passed.
- Rebuilt and recreated only production `public-site` with the Cloudflare
  overlay. Caddy, API, tenant web, database, backup scheduler, and monitor were
  not recreated; all seven services remained healthy.
- External Home, V26 JavaScript/CSS, status health, and Caddy loopback returned
  HTTP 200. Raw UTF-8 verification confirmed the three primary Mongolian area
  labels, OVERVA Apps and independent-supplier labels, and the non-commerce
  sample boundary on the live page.

## D-024 Group structure and public V27 terminology

- Accepted OVERVA Group as an ownership umbrella with three peer operating
  roles: OVERVA Platform, OVERVA Apps, and OVERVA Market. The Group is not a
  shared tenant, database, authorization scope, or permission shortcut.
- Added `OVERVA_GROUP_OPERATING_MODEL_V1.md` and D-024. The model makes OVERVA
  Apps a peer first-party vendor rather than a Platform administrator or Market
  operator, while preserving D-023 equal-participation rules.
- Updated the public Market shell to `Захиалгат ажил ба үйлчилгээ` and aligned
  Product Market categories to Apps, Modules, Connectors, Templates, and AI
  Agents.
- Added a clearly non-operational Market Governance note covering equal supplier
  rules. Product cards, prices, governance action, forum topics, accounts, and
  commerce remain samples or disabled previews.
- Cache-busted the public assets to V27.
- Verified JavaScript syntax, focused Group/public contract tests, all 193
  repository tests, `git diff --check`, and the production-equivalent
  `overva-public-site:v27-local` image build.

## Admin shell V28 bounded contexts

- Renamed the real sign-in and active operating area to OVERVA Platform Admin.
- Added one in-app context switcher for Group overview, Platform, OVERVA Apps,
  and Market Operator; no new service, database, login, or API was introduced.
- Kept Platform as the only operational context. Group is a static boundary
  overview. Apps and Market display truthful planned/no-backend states and state
  that the current Platform token grants no access to them.
- Added responsive context navigation, explicit no-universal-admin guidance,
  cache-busted Platform admin assets, and a regression test preventing fabricated
  Apps or Market API access.
- Verified Platform JavaScript syntax, the focused 6-test control-plane suite,
  all 194 repository tests, and the production-equivalent
  `overva-web:v28-admin-local` image. V28 is not yet deployed.
- Created and verified pre-deployment backup `overva-20260828T122110Z`;
  checksums, PostgreSQL archive listing, and uploads archive inspection passed.
- Rebuilt and recreated only production `public-site` with the Cloudflare
  overlay. Caddy, API, tenant web, database, backup scheduler, and monitor were
  not recreated; all seven services remained healthy.
- External V27 HTML/JS/CSS, the custom-work/service label, Apps, Modules,
  Connectors, Templates, AI Agents, Market Governance note, and sample-truth
  marker were verified. Status health and Caddy loopback returned HTTP 200.

## D-025 bounded admin operating model and simulation

- Defined four administrative contexts: Group oversight, Platform operations,
  OVERVA Apps vendor operations, and Market operator. No universal Group
  super-admin was introduced.
- Added `ADMIN_OPERATING_MODEL_V1.md` with business workspaces, approval gates,
  founder-led operating rules, explicit cross-boundary handoffs, and twenty test
  roles per context.
- Added a test-only control simulation with eighty isolated virtual identities,
  context/permission denial, four-eyes approvals, append-only evidence,
  aggregate-only Group visibility, and a redacted Apps-to-Market release
  contract.
- Focused seven-test simulation suite and the complete 202-test repository
  regression suite pass. No production accounts, APIs,
  databases, permissions, or Market/App backend claims were created.
- Admin shell V29 adds responsive business workspace cards for Group oversight,
  Apps portfolio/build/release/operation, and Market product/custom-work/forum/
  governance responsibilities. The screens remain explicitly no-backend.
- Built `overva-web:v29-admin-local` and verified the cache-busted V11 CSS,
  simulation markers, workspace markup, JavaScript syntax, and diff hygiene.
  V29 is not deployed.

## V30 Platform admin RBAC foundation

- Confirmed the existing Platform backend treated every active
  `platform_admins` record as equally privileged.
- Added migration `0056` with 13 Platform-only permissions, six bounded roles,
  role-permission mappings, attributable role assignments, active-assignment
  indexes, and backward-compatible `platform-owner` assignments for every
  existing active Platform administrator.
- Authentication now derives current roles and permissions on every request;
  organization, adoption, operations, system, AI knowledge/usage, module
  validation, and billing routes enforce scoped permissions server-side.
- Bootstrap ensures its Platform administrator has the owner assignment.
  Platform RBAC contains no Group, Apps, or Market permission.
- Admin UI now fetches only authorized areas, hides unauthorized navigation and
  mutation actions, and preserves permissions after profile updates.
- Focused 23-test checks and the complete 207-test suite pass. A disposable
  PostgreSQL applied migrations `0001–0056` successfully and reported
  `13|6|36` permission/role/mapping counts before being removed.
- Built and inspected `overva-api:v30-admin-rbac-local` and
  `overva-web:v30-admin-rbac-local`. V30 is not deployed.
