# OVERVA Work Log — 2026-08-29

## V34 reviewed Provider lifecycle hardening deployment

- Added migration `0060` and a guarded operator `start-review` transition so a
  Provider application must follow `submitted -> under_review ->
  approved/rejected`; lifecycle skips and operator self-review are rejected.
- Made Customer membership creation concurrency-safe and idempotent. Added
  direct integration coverage for repeated Customer order intent, concurrent
  duplicate Provider applications, participant/operator authorization,
  review/approve/reject/suspend evidence, audit immutability, and guest
  private-URL navigation. A migration regression assertion verifies `0059`
  does not rewrite or delete existing membership rows.
- All 223 repository tests passed. A clean disposable PostgreSQL/API run applied
  `0001–0060` and passed the expanded Market smoke. A headless Edge private URL
  check kept role switching and Provider private navigation hidden for guests.
- Created and verified backup `overva-20260829T061635Z`, applied `0060`, refreshed
  runtime grants, and recreated only API and Public. Production is API V34,
  Public V30, Web/Admin V31, schema `0060`, with all seven services healthy and
  zero Market rows. Local Caddy and external Home/API/Market-auth/assets passed.
- Deployed API image:
  `sha256:50165a48b14af0be442f7186f18c76820959ded82eb4a6fcb5009e4771067188`.
  Deployed Public image:
  `sha256:0f8f65450bd9ebc43d087d463ab3585ef47c81d0626969f1b36b427c1f092719`.

## V33 action-driven Market participation deployment

- Corrected the public Market guest state so unauthenticated visitors see one
  neutral browse context, not Customer/Provider role switching or private work
  queues. Order and Provider actions now continue through Market authentication.
- Customer capacity is created only from an order intent. Provider self-issue
  now returns `MARKET_PROVIDER_APPLICATION_REQUIRED`.
- Added migration `0059` and Provider submit/approve/reject routes. Approval
  requires a separately assigned live Market operator and creates Provider
  membership; rejection does not; operator self-review is denied and audited.
- Updated D-028, architecture, current state, handoff, unit/static tests, and the
  disposable Market integration smoke. Listing, proposal, payment, dispute,
  forum, ranking, and transaction backends were not added.
- All 222 tests passed. Clean PostgreSQL applied `0001–0059`; the expanded API
  smoke passed both decisions, self-review denial, both active participant
  capacities, suspension/reactivation, token boundaries, and immutable audit.
  A 1440×1000 local Edge render passed.
- Created and verified pre-deployment backup `overva-20260829T060223Z`, applied
  migration `0059`, refreshed runtime grants, and recreated only API and Public
  with the Cloudflare overlay. Web/Admin, Caddy, DB, backup, and monitor were not
  recreated.
- Production is API V33, Public V29, Web/Admin V31, and schema `0059`. Market
  remains at zero identities, memberships, Provider applications, operators,
  and audit events. All seven services are healthy; local Caddy, external Home,
  V29 HTML/JS, API, Market auth `401`, app, admin redirect, and status smokes
  passed.
- Deployed API image:
  `sha256:a750213878a4380bcfb7267258526669299f6a135a2e07240b4b033cf308004d`.
  Deployed Public image:
  `sha256:54f2546b9a472b6b871a2a57eaef5b805148e5e70149a51057d5142bf71a16e9`.

## V30 production deployment

- Re-ran the complete API/repository suite: 207 passed, 0 failed.
- Confirmed production was healthy on migration `0055`; recorded previous API
  and Web image IDs for application rollback.
- Created and verified pre-deployment backup
  `overva-20260829T023027Z`. Database dump, uploads archive, metadata checksums,
  PostgreSQL archive listing, and uploads archive inspection passed.
- Applied migration `0056_platform_admin_rbac.sql` with the one-shot migration
  service and refreshed least-privilege runtime grants.
- Verified production catalog counts: 13 Platform permissions, six roles, and
  36 role-permission mappings. The one existing active admin was backfilled as
  one active `platform-owner`.
- Rebuilt and recreated only API and Web with the required Cloudflare Compose
  overlay. Caddy, public-site, database, backup scheduler, and monitor were not
  recreated.
- Corrected two stale production-smoke expectations from schema `0045` to
  `0056` and from six to nine adoption stages. Added a live assertion that the
  existing admin receives `platform-owner` and all 13 permissions.
- Existing-admin control-plane smoke passed. All seven services were healthy;
  local Caddy routing and external admin HTML/JS/CSS, API health, status health,
  public Home, and tenant app health returned HTTP 200.
- Public V27 remains unchanged. Apps and Market continue to have no production
  identity, database, account, commerce, freelance, forum, payment, or operator
  backend.

## Overnight workstation state

- `AutoSleep1AM` remains disabled.
- Balanced-plan AC/DC sleep and hibernate timeouts are both `Never`.
- `ops/keep-awake.ps1` is running in a hidden PowerShell process with PID 1276;
  its heartbeat was current at final verification.
- These settings cannot prevent power loss, hardware failure, forced Windows
  restart, or Docker Desktop failure.

## D-026 and V31 Founder Control

- Accepted layered founder authority: daily Founder operation, separate Market
  customer/provider memberships, external system operation, and offline
  break-glass recovery instead of one universal application super-admin.
- Added migration `0057` with `founder-operator`, two bounded permissions,
  maximum-sixty-minute support grants, and append-only grant lifecycle events.
- Added protected Founder Control, grant issue/revoke, and scoped diagnostic
  snapshot API routes. Grants belong to their issuer and do not create tenant
  identities, bypass tenant APIs, expose raw tenant rows, or permit mutation.
- Added the V31 Founder Control UI with truthful live/preview/planned/external
  capability states and cache-busted V12 admin assets.
- Added an offline production-only owner-recovery script requiring explicit
  confirmation, target email, and reason; it writes Platform and security audit
  evidence.
- All 213 tests passed. Disposable PostgreSQL applied migrations `0001–0057`
  and reported `15|7|53`; founder bootstrap reported two active assignments and
  15 effective permissions. Disposable API integration passed issue, snapshot,
  revoke, inactive denial, and immutable event checks.
- A forced-lockout disposable test deactivated the founder and removed both
  assignments; the offline recovery command restored the active admin, both
  assignments, one Platform audit event, and one security audit event.
- Local V31 images were built and inspected:
  - API `overva-api:v31-founder-control-local` —
    `sha256:3d2e9613b9b83b8a0a9c2177cf42222f4bffffd2f82f15adf6ecb43823b8652b`
  - Web `overva-web:v31-founder-control-local` —
    `sha256:0cd101125d908608867757602862b8871ea67bef1e6277112ad9d72cc5ba3be9`
- Created and verified pre-deployment backup `overva-20260829T031638Z`.
- Applied `0057_founder_control.sql` with the one-shot migration service and
  refreshed least-privilege runtime grants.
- Rebuilt and recreated only API and Web with the Cloudflare Compose overlay.
  Public V27, Caddy, database, backup scheduler, and monitor were not recreated.
- Production verified schema `0057`, catalog counts `15|7|53`, two active
  founder assignments, 15 effective permissions, and Founder Control boundary
  flags denying tenant bypass, Market outcome override, and audit mutation.
- All seven services were healthy. Local Caddy and external admin V12 HTML/JS/
  CSS, API, status, public Home, and tenant app checks returned HTTP 200.
- Production image IDs:
  - API `sha256:3a007b5dbf115b5f0ae15861006b5d9143da62c26f63126296e52391393f050a`
  - Web `sha256:5c2ad3eec659945c60d51ae39bf3d596b0f55736b0bd88bcc17217abed83e737`
- V31 is production-deployed.
