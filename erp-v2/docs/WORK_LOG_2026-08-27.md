# OVERVA Work Log — 2026-08-27

## Public workspace v12 production release

- Accepted D-017: OVERVA Home and Workspace Studio are separate navigation
  levels.
- Added a public portfolio Home for new work, workspace search, recent workspace
  cards, current/next lifecycle context, and text/file/image/system entry paths.
- Preserved the v11 stable browser-local workspace registry, non-destructive
  legacy checkpoint migration, evidence separation, seven delivery gates, and
  no-provisioning boundary.
- Kept marketplace, academy, community, and broad ecosystem claims outside the
  standard customer's seven-stage sidebar.

## Verification evidence

- Full local suite: 181 passing tests.
- Pre-deployment backup: `overva-20260827T023907Z`.
- Backup verification: database, uploads, and metadata checksums passed;
  PostgreSQL custom archive list and uploads tar archive passed.
- Production `public-site` rebuilt and recreated without recreating Caddy, API,
  tenant web, database, backup scheduler, or monitor.
- All production services reported healthy after deployment.
- `https://overva.com/`, v12 CSS, v12 JavaScript, and
  `https://status.overva.com/health` returned HTTP 200.
- Caddy Cloudflare loopback on `127.0.0.1:4180` returned HTTP 200.
- Public HTML exposed `portfolioHome` and `workspaceStudio`; JavaScript exposed
  the v12 registry and Home-to-Workspace route.
- Production JavaScript contained no `/api/public/trials` call and public HTML
  contained no email/password credential fields.
- A 1440x1000 headless Edge production render showed the expected OVERVA Home,
  new-work composer, entry prompts, and workspace-card area.

## Remaining validation

- Conduct human UAT with at least one first-time visitor and one returning
  visitor with multiple saved workspaces.
- Verify wording, card usefulness, mobile behavior, and Home-to-Workspace mental
  model from observed use before adding more global navigation.

## Public workspace v13 navigation release

- Added an explicit `Миний ажлууд` control between the OVERVA brand and
  `Шинэ ажил` inside Workspace Studio.
- Returning Home does not create, replace, or merge a workspace; the existing
  registry and checkpoint remain available through the Home card.
- Kept the workspace selector for switching and `Шинэ ажил` for creating so the
  three navigation outcomes remain distinct.
- Added compact tablet and mobile behavior for the return control.
- Full local suite: 181 passing tests.
- Pre-deployment backup: `overva-20260827T024651Z`, fully verified.
- Production v13 HTML, CSS, JavaScript, public status, and all Compose services
  were healthy after deployment.

## Public workspace v14 Home simplification

- Removed redundant `Нүүр` and `Бүх ажлын өрөө` controls because Home already
  contains both the starting surface and workspace cards on one screen.
- Removed the misleading global `Холболтын ажил` control and its direct
  workspace-creation handler.
- Kept `Одоогийн системээ холбох` as an explicit central start prompt.
- Preserved all existing browser-local checkpoints; no automatic deletion or
  uncertain deduplication was performed.
- Full local suite: 181 passing tests.
- Pre-deployment backup: `overva-20260827T025646Z`, fully verified.
- Production v14 assets, removal markers, public status, and all Compose services
  were healthy after deployment.

## OVERVA Connect OAuth accounts V1 production release

- Accepted D-018: public connector discovery is separate from authenticated
  tenant authorization and never creates a workspace implicitly.
- Added the Google Drive, Google Sheets, and GitHub read-only catalog to public
  Home and the authenticated tenant Connect workspace.
- Added migration `0055` for one-time OAuth state, encrypted tenant connection
  records, append-only connection events, and owner/administrator permission.
- Added OAuth authorization/callback, connect/reconnect, live read-only resource
  listing, disconnect token clearing, audit, and security-audit paths.
- Provider tokens use AES-256-GCM and never return to browser responses or audit
  detail. Missing server OAuth credentials keep a provider visibly unavailable.
- Syntax checks and 187 automated tests passed.
- Pre-deployment backup `overva-20260827T032351Z` passed database, uploads,
  metadata checksum, PostgreSQL archive-list, and uploads archive verification.
- Migration `0055` applied and runtime grants were refreshed. API, tenant web,
  and public-site production images were rebuilt/recreated; all services became
  healthy.
- External `overva.com` v15 Home, `/api/public/connectors`, API health, tenant
  Connect JavaScript/CSS, and a 1440x1000 headless Edge render were verified.
- Google/GitHub OAuth registrations, encryption/provider deployment secrets,
  and live consent remain. Production therefore reports all three providers as
  unavailable instead of presenting a fake working connection.

## Public workspace v16 connector navigation correction

- Added a visible `Холболтууд` control below `Миний ажлууд` in Workspace Studio.
- The control leaves Studio and opens the public connector catalog; it does not
  create, replace, or modify a browser-local workspace.
- Renamed the Home prompt from `Одоогийн системээ холбох` to
  `Системийн зураглал гаргах`, because that prompt starts a discovery workspace
  rather than authorizing an external account.
- Kept provider availability honest: Google Drive, Google Sheets, and GitHub
  remain visibly pending until production OAuth registrations and secrets are
  configured.
- JavaScript syntax, focused connector/navigation tests, and all 187 automated
  tests passed before deployment.
- Rebuilt and recreated only the production `public-site`; the service returned
  healthy and the live HTML/JavaScript exposed v16 assets, the Studio connector
  control, corrected prompt label, and Google Sheets/GitHub catalog entries.

## North Star and market-entry strategy freeze

- Accepted D-019: OVERVA's durable horizon is a connected digital organization,
  production, and market ecosystem with tenant isolation preserved across every
  future cross-organization relationship.
- Froze the initial position around defining an organization's digital need,
  confirming a requirement and preview, selecting a trusted build/developer
  path, and governing delivery through production outcome—not around another
  ERP, a module catalogue, or a generic AI app builder.
- Added `MARKET_ENTRY_STRATEGY_V1.md` as the canonical entry wedge, one-workspace
  product/market proof model, concierge validation target, scope guardrail, and
  future-idea decision filter.
- Kept marketplace maturity explicitly unimplemented and added H-007 for an
  assisted validation target: 10 developer teams, 20 organization discoveries,
  5 request packages, approximately 3 proposals per request, 3 selected
  deliveries, and at least 1 accepted production outcome.
- Deferred a broad app store, hundreds of connectors, consumer commerce,
  research marketplace, industrial exchange, automated escrow, and international
  freelance market until evidence from the active wedge requires them.

## Public workspace v17 organization-first entry

- Repositioned public Home around the question `Байгууллагынхаа ямар ажлыг
  цахимжуулах хэрэгтэй байна?` and explained the immediate outputs before any
  software category or delivery choice.
- Added five bounded organizational problem prompts, four reviewable discovery
  outcomes, and the visible path from problem through requirement, preview, and
  delivery choice to pilot/production.
- Reframed Google Drive, Google Sheets, and GitHub as optional external
  information sources. Kept their real OAuth availability state unchanged.
- Added an informational concierge-pilot panel for developers without claiming
  that public bidding, ranking, escrow, or automated matching exists.
- Kept the stable browser-local workspace registry and internal seven-stage
  delivery lifecycle unchanged; V18 requirement artifacts and V19 request
  packages remain later, evidence-gated releases.
- Fixed mobile Home scrolling and constrained the responsive layout. CDP device
  emulation verified a 390px viewport with `scrollWidth = 390`; 1440px desktop
  and 390px mobile renders were visually reviewed.
- JavaScript syntax, focused public/connector tests, and the full 187-test suite
  passed. Only `public-site` was rebuilt and recreated; the production service
  returned healthy and live assets exposed the V17 positioning.

## Public Home-to-Workspace correction

- Blocked an empty `Хэрэгцээгээ тодорхойлох` submission from creating a blank
  workspace. The CTA is disabled until text exists, and Home explains that the
  organization may write one sentence or choose a bounded example.
- Aligned the Workspace opening message and empty preview with the
  problem → human confirmation → requirement/preview promise.
- Removed the unsafe rule that treated every Excel/CSV mention or upload as an
  employee Smart Import. Employee import now requires people plus data context;
  generic Excel and warehouse problems remain in problem discovery until the
  organization confirms the domain.
- A clean production browser flow verified disabled empty submission, enabled
  text submission, exact text carry-over, visible problem-discovery preview,
  hidden empty state, and human-review status.

## Public V18 structured request intake

- Kept D-019 and the organization-first question, but removed mandatory
  chat-first acquisition. Home now leads with a direct `Хүсэлт гаргах` action.
- Added four clearly labelled `Жишиг хүсэлт` cards for inventory, approval,
  service, and small CRM needs. They are educational templates, not fabricated
  public orders, application counts, prices, ratings, or delivery histories.
- Added one structured browser-local form for request type, business area,
  capabilities, title, problem, desired outcome, budget context, period, and
  intended visibility. Optional OVERVA completeness review remains visibly
  separate from human confirmation.
- Form submission saves `overva.public.request.draft.v1` with
  `published:false`, then opens a separate governed Workspace and carries the
  standardized request package into the existing human-review lifecycle.
- Updated the portfolio contract and market-entry strategy to permit reference
  patterns and browser-local drafts while preserving the no-publication,
  no-proposal, no-payment, and no-automated-marketplace truth boundary.
- JavaScript syntax and all 187 automated tests passed. Only production
  `public-site` was rebuilt and recreated; the service became healthy and live
  `site.css?v=18` / `site.js?v=18` markers were verified.
- A production headless Edge flow verified four templates, an inventory template
  prefilled with four capabilities and AI review off, unpublished local draft
  persistence, and successful handoff into the Workspace conversation.

## Marketplace boundary and demo workflow audit

- Audited the existing tenant user, AI interview/conversation, builder project,
  internal work-order, connector/API-client, and public browser-local workspace
  models before introducing any marketplace persistence.
- Confirmed that no production marketplace records currently exist, so there is
  no present data collision. Reusing tenant `users`, private AI sessions,
  internal `work_orders`, or the API-oriented Developer Platform as marketplace
  aggregates would create identity, authorization, retention, and terminology
  conflicts.
- Identified public V18 draft limitations: one browser-local draft key can be
  overwritten, browser profiles are not authenticated identities, the draft is
  not linked back to a stable workspace identifier, and intended visibility is
  not server-enforced publication authorization.
- Added a test-only in-memory simulator with five customer organizations, ten
  developer organizations, five different request types, fifteen proposals,
  public clarification, private proposal discussion, selected-delivery
  discussion, bilateral agreement, pilot, customer acceptance, production, and
  closure paths.
- Verified denials for unpublished request access, cross-organization ownership,
  proposal spying, duplicate proposals, unselected delivery access, starting a
  pilot before both parties agree, and production deployment before customer
  acceptance. Private evidence is excluded from public snapshots.
- Added `MARKETPLACE_BOUNDARY_AND_DEMO_AUDIT_V1.md` as the implementation
  boundary and follow-up risk register. No production users, providers, orders,
  messages, contracts, payments, or reviews were created.

## Public V19 request-draft identity correction

- Replaced the overwrite-prone single request draft with the versioned
  `overva.public.request.drafts.v2` browser-local registry.
- Added stable request-draft IDs, revision checks, and a two-way traceability
  link: every draft stores its workspace ID and the workspace checkpoint stores
  its request-draft ID.
- Added non-destructive migration of the legacy V18 single draft on the next
  save. Multiple later requests remain separate, and stale same-revision writes
  are rejected by the registry contract.
- Kept the boundary honest: this corrects local traceability only. Browser
  profiles remain unauthenticated, intended visibility remains non-operative,
  and no request is published to developers.
- JavaScript syntax, the focused 11-test public flow, and the complete 191-test
  API/repository suite passed.
- Pre-deployment backup `overva-20260827T065931Z` passed database, uploads,
  metadata checksum, PostgreSQL archive-list, and uploads archive verification.
- Rebuilt and recreated only the production `public-site` with the Cloudflare
  Compose overlay. Caddy, API, tenant web, database, backup scheduler, and
  monitor were not recreated and all services remained healthy.
- The first clean production browser run exposed that the conversation
  checkpoint schema discarded the reverse request-draft link. Added the
  explicit `requestDraftId` checkpoint field, cache-busted conversation memory
  to V15, reran 191 tests, and redeployed the corrected public-site.
- External HTTPS checks returned 200 for Home, V19 site JavaScript, V1 request
  draft registry, V15 conversation memory, and status health. A final clean
  headless Edge flow created two requests with two unique draft IDs and two
  unique workspace IDs, verified every draft/workspace reverse link, confirmed
  no overwrite, and confirmed `published:false` for both drafts.

## Public V20 Home material request correction

- Reproduced the reported Home bug: both `Файл, өгөгдөл` and `Зураг, схем`
  called `beginWorkspaceFromHome` directly, created a blank workspace, opened
  Workspace Studio, and only then launched its material picker.
- Added dedicated Home file and image inputs. Selection now stays on Home and
  opens the structured request form with material name, type, size, kind, and a
  visible browser-local privacy explanation.
- Form confirmation is now the creation boundary. It creates one unpublished
  draft and linked workspace; metadata is recorded in the draft and checkpoint
  evidence, while file content and object URLs are neither uploaded nor stored.
  Removing, cancelling, closing, or pressing Escape clears the pending material.
- Preserved the separate Studio `Материал өгөх` flow for a workspace that
  already exists.
- JavaScript syntax, the focused public-site tests, and all 191 repository tests
  passed. A production-image local Edge E2E verified file selection, image
  selection, no pre-confirmation workspace creation, draft/workspace metadata,
  reverse links, and `published:false`.
- Pre-deployment backup `overva-20260827T071944Z` passed database, uploads,
  metadata checksum, PostgreSQL archive-list, and uploads archive verification.
- Rebuilt and recreated only the production `public-site` with the Cloudflare
  overlay. All production services remained healthy. Live V20 HTML/JS/CSS,
  status health, and loopback origin returned 200.
- A clean production Edge E2E selected a CSV and an SVG through their real file
  inputs. Both stayed on Home, kept Studio hidden, opened the request dialog,
  rendered the correct material summary, and left the workspace registry
  unchanged until confirmation.

## Public V21 request-market Home

- Replaced the oversized chat-first Home hierarchy with a compact request-board
  hierarchy: `Бүх хүсэлт`, `Миний хүсэлт`, `Миний ажил`, `Дүрэм`, search,
  bounded categories, status tabs, and one explicit `Хүсэлт гаргах` action.
- Converted the four educational examples into readable one-row request
  summaries. Each remains explicitly labelled `ЖИШИГ`; the screen states that
  all four are not real published orders.
- Connected `Миний хүсэлт` to the existing separate browser-local request-draft
  registry and `Миний ажил` to the existing workspace registry. No duplicate
  customer/developer memory or cross-workspace evidence store was introduced.
- Added a concise Rules surface with human-gated request -> requirement ->
  preview -> acceptance -> production sequencing and explicit disclosure that
  proposals, escrow/payment, automatic contracts, and arbitration are not yet
  production capabilities.
- Preserved structured request intake, optional AI completeness review,
  file/image metadata flow, unpublished-draft creation, and Workspace Studio.
- JavaScript syntax, the focused 11-test public flow, and all 191 repository
  tests passed. The production image built successfully. A desktop headless
  Edge E2E verified initial rendering, four sample rows, category filtering,
  Rules and My Requests navigation, empty state, request dialog, and both
  attachment entry actions.
- Pre-deployment backup `overva-20260827T074109Z` passed database, uploads, and
  metadata verification. Only `public-site` was recreated; Caddy, API, tenant
  web, database, backup scheduler, and monitor remained healthy.
- External Home, V21 JavaScript, V21 CSS, status health, loopback origin, four
  sample-row markers, market navigation, Rules panel, and create-request action
  all returned the expected production result.

## Public V22 role-separated Market shell and operating model

- Split the ambiguous Market navigation into explicit customer and provider
  work contexts. Customer workspaces are now `Миний төслүүд`; provider
  proposals and delivery jobs have separate truthful empty states.
- Added provider execution rules and an on-screen lifecycle showing selection,
  agreement, delivery evidence, customer acceptance, production, and review.
  No live provider, proposal, payment, escrow, or review capability is claimed.
- Accepted D-020 and added `MARKETPLACE_OPERATING_MODEL_V1.md` as durable memory
  for the role split, delivery control record, verified review gate, and the
  experimental requirement-service plus 8-10% success-fee hypothesis.
- Extended the five-customer/ten-provider test-only simulation so customer
  acceptance requires provider evidence, early/fabricated reviews fail, each
  party may submit one verified review after closure, and fee calculations are
  experimental quotes rather than payment processing.
- JavaScript syntax and all 191 tests passed; the production public-site image
  built successfully. Verified backup `overva-20260827T081501Z` was created.
  Only `public-site` was recreated with the Cloudflare overlay. Every production
  service is healthy, and external Home, V22 JS/CSS, status, loopback origin,
  customer/provider role markers, and provider delivery panel returned 200 and
  the expected content.

## Public V22.2 customer-language and lifecycle clarification

- Preserved all existing device-local project checkpoints and added a visible
  `Энэ төхөөрөмжид хадгалсан туршилтын төсөл` marker to every saved customer
  project card. No user checkpoint or request draft was deleted or migrated.
- Reworded the customer request, project, privacy, attachment, and Rules views
  in plain Mongolian so users do not need to understand browser-local,
  workspace, requirements specification, developer proposal, escrow, or
  production-capability terminology.
- Expanded the customer Rules sequence to request -> confirmed requirement ->
  proposal -> selection -> agreement -> execution -> acceptance -> operational
  launch -> review. The screen still truthfully states that real proposals,
  payments, automatic contracts, and dispute resolution are not yet live.
- JavaScript syntax and all 191 tests passed. The production image built, and
  verified backup `overva-20260827T084207Z` passed checksums, PostgreSQL archive
  listing, and uploads archive inspection. Only `public-site` was recreated;
  every production service is healthy. External V22.2 HTML/JS/CSS markers and
  `status.overva.com` returned the expected live result.

## Public V23 request/project boundary correction

- Separated the customer's pre-award request lifecycle from post-agreement
  projects. Saving a request now creates only a browser-local draft; it neither
  creates a project nor opens Workspace Studio automatically.
- Added a request-detail surface for reviewing the structured request package.
  An optional OVERVA review action creates a separately labelled requirement-
  refinement trial room and links it through `reviewWorkspaceId`.
- Reserved `My Projects` for work that has passed provider selection and
  bilateral agreement. Moved existing device-local checkpoints to a separate
  `Trial Rooms` surface without deleting user data.
- Added distinct inventory, approval, service, and CRM review-room previews so
  the four examples no longer open the same generic problem-map experience.
- Added required acceptance criteria, at-least-one-capability validation,
  conditional budget-detail validation, honest publication copy, result counts,
  and filter empty states to structured request intake.
- Accepted D-021 and updated the marketplace operating model to make the
  request -> selection/agreement -> project creation boundary durable.
- JavaScript syntax and all 191 tests passed. The production-equivalent
  `public-site` image built successfully. A clean headless Edge run verified
  category filtering, a top-positioned seeded form, draft-only save, explicit
  review-room creation, a type-specific inventory preview, an empty projects
  state, and retained trial-room cards.
- Pre-deployment backup `overva-20260827T093221Z` passed checksums,
  PostgreSQL archive listing, and uploads archive inspection. Only the
  production `public-site` was recreated with the Cloudflare overlay; API,
  tenant web, database, Caddy, backup scheduler, and monitor remained healthy.
  External Home, V23 JavaScript/CSS, request registry, and status health all
  returned 200, and the live HTML/JavaScript contained the V23 request-detail,
  trial-room, acceptance-criteria, and review-workspace markers.

## Public V24 optional joint-clarification production release

- Accepted D-022: a customer never has to enter a trial room, AI conversation,
  preview, or fixed OVERVA lifecycle to choose a provider, agree, or start a
  project.
- Removed `Trial Rooms`, customer rules, and provider execution rules from
  primary navigation. Legacy browser-local checkpoints remain recoverable from
  secondary help and are relabelled as earlier experiments rather than projects.
- Added role-specific customer/provider Home language. Provider mode now states
  plainly that no live work is available and that the four visible rows are
  examples, not orders.
- Rewrote proposal and delivery empty states in plain Mongolian and removed
  provider Market UI references to provider identity, proposal backend, scope,
  milestones, production capability, and verified review.
- Added the optional post-selection branch: the provider may propose `Joint
  Clarification`, the customer accepts it, and both parties may skip it and move
  directly to agreement. The screen explains that this is for mapping the real
  workflow and acceptance outcome, not writing code.
- Preserved the V23 draft/project boundary and all browser-local records. No
  provider, proposal, selection, agreement, invitation, payment, or review
  backend was added or claimed.
- JavaScript syntax, the focused 11-test public flow, and all 191 repository
  tests passed. The production-equivalent `overva-public-site:v24-local` image
  built successfully. Desktop customer, provider work-finder, and provider
  delivery views were rendered and visually reviewed with local headless Edge.
  A disposable container returned HTTP 200 for Home and health and exposed the
  V24 assets, optional-clarification markup, and provider delivery flow. The
  container was stopped after verification.
- Verified pre-deployment backup `overva-20260827T114821Z` passed database,
  uploads, metadata checksum, PostgreSQL archive-list, and uploads archive
  verification.
- Rebuilt the production V24 image and recreated only `public-site` with the
  Cloudflare overlay. API, web, database, Caddy, backup scheduler, and monitor
  were not recreated; every production service remained healthy.
- Caddy loopback, external Home, V24 JavaScript/CSS, and status health returned
  HTTP 200. Live HTML exposed the optional clarification and provider delivery
  flow. A bounded navigation check confirmed three primary customer items and
  three primary provider items, with legacy trials and rules absent from both
  primary navigation groups.
