"use strict";
labels["ai-director"]="AI Director";state.aiDirector=null;const renderBeforeAi=render;render=function(){if(state.view!=="ai-director")return renderBeforeAi();renderAiDirector()};
const adjustBeforeAi=adjustModuleNavigation;adjustModuleNavigation=function(){adjustBeforeAi();if(state.user)$("#aiDirectorNav")?.classList.toggle("hidden",!["director","chief_engineer","accountant"].includes(state.user.role))};
async function loadAiDirector(){try{state.aiDirector=await api("/api/ai-director/briefs");render()}catch(error){toast(error.message,true)}}
function renderAiDirector(){const d=state.aiDirector;if(!d){$("#pageContent").innerHTML=header("DECISION INTELLIGENCE","AI Director","Удирдлагын brief-ийг ачаалж байна...");queueMicrotask(loadAiDirector);return}const latest=d.items[0];$("#pageContent").innerHTML=`${header("DECISION INTELLIGENCE","AI Director","ERP-ийн бодит өгөгдлөөс эрсдэл, нотолгоо, хийх үйлдлийг эрэмбэлсэн удирдлагын brief.",`<button class="primary" id="generateAiBrief">✦ Шинэ brief гаргах</button>`)}${latest?`<section class="ai-brief-hero"><div><span class="eyebrow">${date(latest.period_start)} – ${date(latest.period_end)}</span><h2>${esc(latest.headline)}</h2><p>${latest.recommendations.length} зөвлөмж · ${dateTime(latest.created_at)}</p></div><div class="ai-health">${latest.health_score}<small>HEALTH SCORE</small></div></section><div class="ai-list">${latest.recommendations.map(x=>`<article class="ai-advice ${x.priority}"><span class="ai-priority">${esc(x.priority)}</span><div><h3>${esc(x.title)}</h3><p class="ai-evidence">Нотолгоо: ${esc(x.evidence)}</p><p class="ai-action">Хийх ажил: ${esc(x.action)}</p></div><button class="secondary" data-ai-view="${esc(x.view)}">Нээх →</button></article>`).join("")}</div>`:empty("Brief үүсээгүй байна","ERP-ийн одоогийн өгөгдөл дээр үндэслэсэн анхны brief-ээ гаргана уу.")}<section class="ai-history"><h2>Brief түүх</h2>${d.items.map(x=>`<div class="ai-history-item"><div><strong>${esc(x.headline)}</strong><small>${dateTime(x.created_at)} · ${x.recommendations.length} зөвлөмж</small></div><b>${x.health_score}/100</b></div>`).join("")||"<small>Түүх алга.</small>"}<div class="ai-mode">Local evidence engine · Өгөгдөл гаднын AI үйлчилгээ рүү илгээгдэхгүй.</div></section>`}
document.addEventListener("click",async event=>{const go=event.target.closest("[data-ai-view]");if(go)setView(go.dataset.aiView);if(event.target.closest("#generateAiBrief")){try{await api("/api/ai-director/briefs/generate",{method:"POST",body:"{}"});state.aiDirector=null;await loadAiDirector();toast("Шинэ удирдлагын brief бэлэн боллоо")}catch(error){toast(error.message,true)}}});
