"use strict";
labels.fleet="GPS / Fleet";state.fleetData=null;state.fleetHistory=null;let fleetMapInstance=null,fleetRouteLayer=null;
const renderBeforeFleet=render;render=function(){if(state.view!=="fleet")return renderBeforeFleet();renderFleetView()};
async function loadFleet(){try{state.fleetData=await api("/api/gps/overview");render()}catch(error){toast(error.message,true)}}
function fleetDateTime(value){return value?dateTime(value):"Мэдээлэл ирээгүй"}
function renderFleetView(){const d=state.fleetData;if(!d){$("#pageContent").innerHTML=header("CONNECTED OPERATIONS","GPS / Fleet","Тээврийн хэрэгслийн мэдээллийг ачаалж байна...");queueMicrotask(loadFleet);return}const online=d.vehicles.filter(x=>x.online).length,moving=d.vehicles.filter(x=>x.online&&Number(x.speed_kph)>3).length,offline=d.vehicles.filter(x=>x.device_id&&!x.online).length,canManage=["director","chief_engineer"].includes(state.user.role);$("#pageContent").innerHTML=`${header("CONNECTED OPERATIONS","GPS / Fleet","Машины байрлал, төлөв, маршрут болон geofence үйл явдлыг нэг дор хянана.",`<button class="secondary" id="fleetRefresh">↻ Шинэчлэх</button>`)}<div class="fleet-stats"><article class="fleet-stat"><span>Нийт машин</span><strong>${d.vehicles.length}</strong></article><article class="fleet-stat"><span>Онлайн</span><strong>${online}</strong></article><article class="fleet-stat"><span>Хөдөлгөөнтэй</span><strong>${moving}</strong></article><article class="fleet-stat"><span>Оффлайн</span><strong>${offline}</strong></article></div><div class="fleet-grid"><section class="fleet-panel"><div class="fleet-panel-head"><h2>Тээврийн хэрэгслүүд</h2><small>Онлайн: сүүлийн 5 минут</small></div><div class="fleet-list">${d.vehicles.length?d.vehicles.map(v=>`<article class="fleet-vehicle"><div><h3>${esc(v.plate_number)} · ${esc(v.asset_name)}</h3><div class="fleet-meta"><span>${esc([v.make,v.model].filter(Boolean).join(" ")||v.asset_code)}</span><span>Жолооч: ${esc(v.driver_name||"—")}</span><span>${Number(v.odometer_km).toLocaleString()} км</span><span>GPS: ${esc(v.device_uid||"холбоогүй")}</span></div><div class="fleet-meta"><span>${v.latitude?`${Number(v.latitude).toFixed(5)}, ${Number(v.longitude).toFixed(5)}`:"Байршил ирээгүй"}</span><span>${fleetDateTime(v.recorded_at)}</span></div></div><div class="fleet-actions"><span class="fleet-live ${v.online?"online":""}"><i></i>${v.online?`${Number(v.speed_kph||0).toFixed(0)} км/ц`:"Оффлайн"}</span>${v.latitude?`<button data-fleet-focus="${v.id}">Map</button><button data-fleet-history="${v.id}">24ц маршрут</button>`:""}${canManage&&v.device_id?`<button data-rotate-device="${v.device_id}">Key солих</button>`:""}</div></article>`).join(""):empty("Fleet машин бүртгэгдээгүй","Хөрөнгийн бүртгэлээс тээврийн хэрэгсэл сонгон Fleet-д нэмнэ үү.")}</div></section><section class="fleet-panel"><div class="fleet-panel-head"><h2>${state.fleetHistory?`${esc(state.fleetHistory.vehicle.plate_number)} маршрут`:"Live map"}</h2></div><div class="fleet-map" id="fleetMap"></div><div class="fleet-route-info" id="fleetRouteInfo">${state.fleetHistory?`${state.fleetHistory.items.length} GPS цэг · ${dateTime(state.fleetHistory.from)} – ${dateTime(state.fleetHistory.to)}`:"Машины хамгийн сүүлийн байршил"}</div><div class="fleet-panel-head"><h2>Geofence event</h2></div><div class="fleet-events">${d.events.length?d.events.map(e=>`<div class="fleet-event"><i></i><div><strong>${esc(e.plate_number)} · ${e.event_type==="entered"?"Бүсэд орсон":"Бүсээс гарсан"}</strong><small>${esc(e.geofence_name)} · ${dateTime(e.occurred_at)}</small></div></div>`).join(""):empty("Event алга","GPS машин geofence хил нэвтрэхэд энд бүртгэгдэнэ.")}</div></section></div>${canManage?fleetManagementForms(d):""}`;queueMicrotask(initFleetMap)}
function fleetManagementForms(d){const available=state.assets.filter(a=>!d.vehicles.some(v=>v.asset_id===a.id)),noDevice=d.vehicles.filter(v=>!v.device_id);return `<div class="fleet-forms"><form class="fleet-form" id="fleetVehicleForm"><h3>Fleet машин нэмэх</h3><label>Хөрөнгө<select name="assetId" required>${available.map(a=>`<option value="${a.id}">${esc(a.code)} · ${esc(a.name)}</option>`).join("")}</select></label><div class="fleet-form-grid"><label>Улсын дугаар<input name="plateNumber" required placeholder="ДОА 1234"></label><label>Жолооч<select name="driverUserId"><option value="">Сонгохгүй</option>${state.assignableUsers.map(u=>`<option value="${u.id}">${esc(u.full_name)}</option>`).join("")}</select></label><label>Марк<input name="make"></label><label>Загвар<input name="model"></label><label>Гүйлт, км<input name="odometerKm" type="number" min="0" value="0"></label><label>Бак, литр<input name="fuelCapacityL" type="number" min="1"></label></div><button class="primary">Fleet-д нэмэх</button></form><form class="fleet-form" id="gpsDeviceForm"><h3>GPS төхөөрөмж холбох</h3><label>Машин<select name="vehicleId" required>${noDevice.map(v=>`<option value="${v.id}">${esc(v.plate_number)}</option>`).join("")}</select></label><div class="fleet-form-grid"><label>Device UID / IMEI<input name="deviceUid" required></label><label>Provider<input name="provider" value="generic" required></label></div><button class="primary">Төхөөрөмж бүртгэх</button><div class="device-key-note">API key зөвхөн нэг удаа харагдана. Provider нь POST /api/gps/ingest рүү X-Device-Key header-ээр илгээнэ.</div></form><form class="fleet-form" id="geofenceForm"><h3>Geofence нэмэх</h3><label>Нэр<input name="name" required></label><div class="fleet-form-grid"><label>Latitude<input name="latitude" id="geofenceLat" type="number" step="0.000001" required></label><label>Longitude<input name="longitude" id="geofenceLng" type="number" step="0.000001" required></label><label>Радиус, метр<input name="radiusM" type="number" min="10" value="500" required></label><label>Өнгө<input name="color" type="color" value="#f59e0b"></label></div><button class="primary">Geofence үүсгэх</button><div class="device-key-note">Дээрх map дээр дарж төв цэгийг сонгож болно.</div></form></div>`}
function initFleetMap(){const d=state.fleetData,el=$("#fleetMap");if(!d||!el||typeof L==="undefined")return;if(fleetMapInstance)fleetMapInstance.remove();const center=state.mapData?[Number(state.mapData.settings.map_center_lat),Number(state.mapData.settings.map_center_lng)]:[48.0726,114.5356];fleetMapInstance=L.map(el).setView(center,13);L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png",{maxZoom:19,attribution:"© OpenStreetMap"}).addTo(fleetMapInstance);for(const g of d.geofences)L.circle([Number(g.center_lat),Number(g.center_lng)],{radius:g.radius_m,color:g.color,fillOpacity:.08}).addTo(fleetMapInstance).bindPopup(`<b>${esc(g.name)}</b><br>${g.radius_m} м`);for(const v of d.vehicles)if(v.latitude)L.marker([Number(v.latitude),Number(v.longitude)],{icon:markerIcon(v.online?"#0f9f6e":"#64748b","▰")}).addTo(fleetMapInstance).bindPopup(popupHtml(v.plate_number,`${v.driver_name||"Жолоочгүй"} · ${Number(v.speed_kph||0).toFixed(0)} км/ц`,v.online?"Онлайн":"Оффлайн"));if(state.fleetHistory?.items.length){const points=state.fleetHistory.items.map(p=>[Number(p.latitude),Number(p.longitude)]);fleetRouteLayer=L.polyline(points,{color:"#246bea",weight:5,opacity:.8}).addTo(fleetMapInstance);fleetMapInstance.fitBounds(fleetRouteLayer.getBounds(),{padding:[25,25]})}fleetMapInstance.on("click",e=>{if($("#geofenceLat"))$("#geofenceLat").value=e.latlng.lat.toFixed(6);if($("#geofenceLng"))$("#geofenceLng").value=e.latlng.lng.toFixed(6)})}
async function fleetReload(message){state.fleetData=null;await loadFleet();if(message)toast(message)}
document.addEventListener("click",async event=>{const history=event.target.closest("[data-fleet-history]"),focus=event.target.closest("[data-fleet-focus]"),rotate=event.target.closest("[data-rotate-device]");try{if(event.target.closest("#fleetRefresh")){state.fleetHistory=null;await fleetReload("GPS мэдээлэл шинэчлэгдлээ")}if(history){state.fleetHistory=await api(`/api/gps/vehicles/${history.dataset.fleetHistory}/history`);render()}if(focus&&fleetMapInstance){const v=state.fleetData.vehicles.find(x=>x.id===focus.dataset.fleetFocus);if(v?.latitude)fleetMapInstance.setView([Number(v.latitude),Number(v.longitude)],17)}if(rotate&&confirm("GPS API key-г солих уу? Хуучин key шууд хүчингүй болно.")){const out=await api(`/api/gps/devices/${rotate.dataset.rotateDevice}/rotate-key`,{method:"POST",body:"{}"});prompt("Шинэ API key — одоо хуулж аюулгүй хадгална уу:",out.apiKey)}}catch(error){toast(error.message,true)}});
document.addEventListener("submit",async event=>{const form=event.target;if(!["fleetVehicleForm","gpsDeviceForm","geofenceForm"].includes(form.id))return;event.preventDefault();const v=Object.fromEntries(new FormData(form));try{if(form.id==="fleetVehicleForm"){v.driverUserId=v.driverUserId||null;v.fuelCapacityL=v.fuelCapacityL||null;await api("/api/gps/vehicles",{method:"POST",body:JSON.stringify(v)});await fleetReload("Fleet машин нэмэгдлээ")}if(form.id==="gpsDeviceForm"){const out=await api("/api/gps/devices",{method:"POST",body:JSON.stringify(v)});await fleetReload("GPS төхөөрөмж холбогдлоо");prompt("API key — зөвхөн одоо харагдана. Хуулж хадгална уу:",out.apiKey)}if(form.id==="geofenceForm"){await api("/api/gps/geofences",{method:"POST",body:JSON.stringify(v)});await fleetReload("Geofence үүслээ")}}catch(error){toast(error.message,true)}});
