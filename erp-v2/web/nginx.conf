map $http_x_forwarded_for $overva_forwarded_for {
  default $http_x_forwarded_for;
  "" $remote_addr;
}

map $http_x_forwarded_proto $overva_forwarded_proto {
  default $http_x_forwarded_proto;
  "" $scheme;
}

server {
  listen 80;
  server_name _;
  client_max_body_size 11m;

  add_header X-Content-Type-Options "nosniff" always;
  add_header Referrer-Policy "no-referrer" always;
  add_header X-Frame-Options "DENY" always;
  add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https://tile.openstreetmap.org https://*.tile.openstreetmap.org; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'" always;

  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_pass http://api:4100;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $overva_forwarded_for;
    proxy_set_header X-Forwarded-Proto $overva_forwarded_proto;
  }

  location /health {
    proxy_pass http://api:4100/health;
  }

  location = /service-worker.js {
    add_header Cache-Control "no-cache";
    try_files $uri =404;
  }

  # Versioned public names prevent a previously cached SPA fallback from being
  # served as JavaScript/CSS after a new production asset is introduced.
  location = /organization-structure-hub.js {
    add_header Cache-Control "public, max-age=14400, immutable";
    try_files /structure-hub.js =404;
  }

  location = /organization-structure-hub.css {
    add_header Cache-Control "public, max-age=14400, immutable";
    try_files /structure-hub.css =404;
  }

  # Static script and stylesheet requests must never receive the SPA shell.
  # A missing asset is a deployment error and must be visible as a 404.
  location ~* \.(?:js|css)$ {
    try_files $uri =404;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
